#include <Arduino.h>


void setup() {
  //  pinMode(LEDB, OUTPUT);
//    digitalWrite(LEDB, HIGH);
}

bool ledState = false;
void loop() {
    //    ledState = !ledState;
   // digitalWrite(LEDB, ledState ? HIGH : LOW);

  delay(1000);
}

